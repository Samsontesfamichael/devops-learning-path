### 05: Git and Version Control

Mastering version control workflows to track infrastructure configurations safely.

### Learning Checklist

*   Basic staging and committing workflows (`add`, `commit`, `status`)
*   Branching strategies for team code review (Feature branching)
*   Rewriting repository history safely via `git rebase`
*   Resolving code line merge conflicts manually