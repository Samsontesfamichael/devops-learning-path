### 03: Networking Foundations

A deep dive into how computers communicate across local networks and the internet.

### Learning Checklist

*   The OSI and TCP/IP networking models
*   Subnetting, IP routing tables, and gateway setups
*   Troubleshooting packet paths using `ping`, `traceroute`, and `netstat`
*   Configuring secure cryptographic connections via SSH keys